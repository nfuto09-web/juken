---
description: 学習進捗ダッシュボードを表示する
---

`scripts/generate_dashboard.py` を実行し、`dashboard/today.md` を最新化した上で、
その内容をそのままユーザーに表示してください。スクリプトが失敗する場合は、
`data/study_log/` と `anki/cards.csv` の状態を確認し、原因を報告してください。
